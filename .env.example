# ============================================
# MangeXis Secure Environment Variables
# ============================================
# CRITICAL SECURITY WARNING:
# - NEVER commit real values to Git!
# - Configure these in Netlify Dashboard only
# - Use strong, randomly generated values
# ============================================

# ============================================
# JWT Security (REQUIRED)
# ============================================
# Generate: openssl rand -base64 64
# Or: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET=REPLACE_WITH_64_CHARACTER_RANDOM_STRING

# ============================================
# Admin Account (Full Access)
# ============================================
ADMIN_USERNAME=admin
# Generate hash: node netlify/functions/generate-password-hash.js YourStrongPassword123!
ADMIN_PASSWORD_HASH=$2a$12$REPLACE_WITH_REAL_BCRYPT_HASH

# ============================================
# Fansub Account (Chapter Upload Only)
# ============================================
FANSUB_USERNAME=fansub
# Generate hash: node netlify/functions/generate-password-hash.js FansubSecurePass456!
FANSUB_PASSWORD_HASH=$2a$12$REPLACE_WITH_REAL_BCRYPT_HASH

# ============================================
# GitHub Integration (for data storage)
# ============================================
GITHUB_TOKEN=your_github_personal_access_token
REPO_OWNER=Enmxy
REPO_NAME=Mangexis
